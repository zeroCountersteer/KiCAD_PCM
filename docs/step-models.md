# STEP model stage

`step-model` validates STEP headers, preserves source provenance, recognizes common units, scans Cartesian points for a deterministic nanometre bounding box, and computes a geometry fingerprint. It is deliberately not a CAD kernel. Associations require manifest evidence; filename resemblance alone is not sufficient.
