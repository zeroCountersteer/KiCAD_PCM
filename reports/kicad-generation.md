# KiCad generation report

Generated with KiCad 10.0.4-compatible modern S-expressions (`kicad_symbol_lib` version `20231120`; `.kicad_mod` version `20231120`).

The representative TI output contains one symbol library and six footprints:

* symbols: `SN7400_D_14`, `SN74ABT16245A_DGG_48`, and a deterministic collision-suffixed sample symbol
* footprints: `D14`, `D14-M`, `D14-L`, `DGG48`, `DGG48-M`, and `DGG48-L`
* KiCad CLI upgrade validation: successful for the symbol library and footprint directory
* SVG preview validation: successful for the generated symbol library and footprints

All unresolved BXL electrical types remain `unspecified`. Courtyards and 3D model blocks are omitted unless a confidently mapped source construct is available. Unknown or unsupported source semantics are reported in `data/generated/kicad/reports/kicad-generation.md`.
